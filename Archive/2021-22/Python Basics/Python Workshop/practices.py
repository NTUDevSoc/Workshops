print("this is another file!")

# This is a single line comment
# There are only single line comments in python
# UNLESS your IDE/Editor has built-in support for other forms


ThisIsUpperCamelCase = "used for class names"
thisIsLowerCamelCase = "used for function names"
this_is_lowercase_with_underscores = "used for variables"
THIS_IS_UPPERCASE_WITH_UNDERSCORES = "used for constants"
_this_is_a_private_variable = "use for private variables if doing object orientated programming"

radius_of_circle = 10
r = 10

this_variable = 2

some_variable = 100
def aFunction():
    this_variable = 10
    print(str(some_variable + this_variable))

print(str(some_variable + this_variable))

bmi = 42

if bmi > 25 and bmi < 32: print("")
if bmi > 25 or bmi < 32: print("")
if not(bmi > 25 and bmi < 32): print("")

# > more than
# < less than
# <= less than or equal to
# >= more than or equal to
# == equal to
# != not equal to